# Copy this file to .env and fill in your own values.
# TiDB Cloud connection string (MySQL-compatible). Get this from your
# TiDB Cloud cluster's "Connect" panel — make sure to URL-encode any
# special characters in the password, and append ?sslaccept=strict.
#
# Example:
# DATABASE_URL="mysql://USER.root:PASSWORD@gateway01.region.prod.aws.tidbcloud.com:4000/notice_board?sslaccept=strict"

DATABASE_URL=""
